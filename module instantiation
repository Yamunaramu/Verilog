module top_module ( input a, input b, output out );
	// instantiation by explicit declaration
    
    mod_a instance1(.in1(a),.in2(b),.out(out));
endmodule

module top_module ( input a, input b, output out );
	// instantiation by position
    
    mod_a instance1(a,b,out);
endmodule
